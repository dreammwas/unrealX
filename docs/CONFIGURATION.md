# Configuration

Persistent configuration is stored under `/data/adb/unrealX/config/`. The module uses simple validated key/value files for maximum portability. Supported keys include `profile`, `adaptive_engine`, `thermal_protection`, `frame_strategy`, `logging`, `safe_mode`, and `game_auto_detection`.

Malformed or unknown values are not used for privileged changes.
