# Development

Architecture is deliberately capability-first. Add a feature only after answering: what changes, why it helps, which devices expose it, how it is verified, and how it is rolled back.

Optional C and Rust sources are included for target-specific native builds; the installable baseline does not ship unverified host-built ELF binaries.
